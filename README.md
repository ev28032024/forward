# 📡 Discord → Telegram Forward Monitor

Утилита отслеживает выбранные каналы Discord и пересылает новые сообщения или закрепы в Telegram. Подходит для анонсов,
технических обновлений и любых объявлений, которые нельзя пропустить.

---

## Содержание

- [Возможности](#features)
- [Что понадобится](#requirements)
- [Установка и проверка](#setup)
- [Настройка `config.yml`](#configuration)
  - [Создание конфигурации](#1-создание-конфигурации)
  - [Обязательные параметры](#2-обязательные-параметры)
  - [Фильтры](#3-настройка-фильтров-опционально)
  - [Кастомизация текста](#4-кастомизация-текста-опционально)
  - [Привязка каналов](#5-связка-каналов-discord-с-чатами-telegram)
- [Запуск](#run)
- [Состояние и надёжность](#reliability)
- [Полезные советы](#tips)
- [Вопросы](#questions)

---

<a id="features"></a>
## ✨ Возможности

| Возможность            | Описание                                                                                           |
| ---------------------- | -------------------------------------------------------------------------------------------------- |
| Мониторинг анонсов     | Отправляет новые сообщения из выбранных каналов Discord в нужные чаты Telegram.                    |
| Мониторинг закрепов    | Отслеживает появление новых «пинов» и присылает уведомление.                                       |
| Красивое оформление    | Приводит Discord-разметку в читабельный вид, корректно обрабатывает упоминания и добавляет ссылку. |
| Передача вложений      | Отправляет изображения, документы, видео и аудио сразу после текста.                               |
| Гибкие фильтры         | Настраиваются whitelist/blacklist, ограничения по авторам и типам контента.                         |
| Кастомизация сообщений | Добавляет заголовки, подписи и замены текста.                                                      |
| Антиспам-задержки      | Делает случайные паузы между отправками, чтобы не упереться в лимиты Telegram.                     |
| Повторная отправка     | При временных ошибках выполняет повторную попытку с учётом `Retry-After`.                          |

---

<a id="requirements"></a>
## 🧰 Что понадобится

1. **Python 3.10+** — удобно ставить через `pyenv` или пакетный менеджер.
2. **Discord user token** с правом читать выбранные каналы (учётка должна состоять на сервере и иметь «Read Message History»).
3. **Токен Telegram-бота**, созданного через [@BotFather](https://t.me/BotFather).
4. **ID или username** Telegram-чатов и каналов, куда пересылать сообщения (`-1001234567890`, `@my_channel` и т.д.).

> 💡 Для запуска на сервере удобно использовать `tmux`/`screen` или оформить systemd-сервис.

---

<a id="setup"></a>
## ⚙️ Установка и проверка

```bash
git clone <URL_ВАШЕГО_ФОРКА_ИЛИ_РЕПО>
cd forward

python -m venv .venv
source .venv/bin/activate

pip install -e .[dev]
pytest
```

> Команда `pip install -e .[dev]` установит зависимости приложения и инструменты для разработки и тестирования.

---

<a id="configuration"></a>
## 🧾 Настройка `config.yml`

### 1. Создание конфигурации

```bash
cp config.example.yml config.yml
```

### 2. Обязательные параметры

```yaml
discord_token: "DISCORD_USER_TOKEN"
telegram_token: "TELEGRAM_BOT_TOKEN"
telegram_chat_id: "@default_channel_or_chat_id"  # используется, если у канала нет собственного чата
poll_interval: 120
state_file: "monitor_state.json"
min_message_delay: 1.0
max_message_delay: 3.0
```

| Параметр                | Обязателен | Значение по умолчанию | Назначение                                                         |
| ----------------------- | ---------- | --------------------- | ------------------------------------------------------------------ |
| `discord_token`         | ✅         | —                     | Пользовательский токен Discord, от имени которого читаются каналы. |
| `telegram_token`        | ✅         | —                     | Токен Telegram-бота, который пересылает сообщения.                 |
| `telegram_chat_id`      | ✅         | —                     | Чат или канал по умолчанию для всех пересылок.                     |
| `announcement_channels` | ❌         | `[]`                  | Список каналов, из которых брать обычные сообщения.                |
| `pinned_channels`       | ❌         | `[]`                  | Каналы, где отслеживаются новые закрепы.                           |
| `filters`               | ❌         | пустые списки         | Глобальные фильтры по тексту, авторам и типам вложений.            |
| `customization`         | ❌         | пустые списки         | Глобальные заголовки, подписи и замены текста.                     |
| `poll_interval`         | ❌         | `300`                 | Пауза между проходами по каналам (в секундах).                     |
| `state_file`            | ❌         | `monitor_state.json`  | Файл, где хранится ID последнего обработанного сообщения.         |
| `min_message_delay`     | ❌         | `0.5`                 | Минимальная задержка между отправкой сообщений и вложений.        |
| `max_message_delay`     | ❌         | `2.0`                 | Максимальная задержка между отправкой сообщений и вложений.       |

> ⚠️ Значение `max_message_delay` должно быть **не меньше** `min_message_delay`, иначе монитор остановится с ошибкой конфигурации.

### 3. Настройка фильтров (опционально)

```yaml
filters:
  whitelist:
    - "airdrop"
    - "update"
  blacklist:
    - "maintenance"
  allowed_senders:
    - "ProjectTeam"      # имя или ник автора
    - "1234567890123456" # ID пользователя
  blocked_senders:
    - "Bad Actor"
  allowed_types:
    - text
    - image
  blocked_types:
    - audio
```

| Ключ              | Что делает                                                                                                 |
| ----------------- | ----------------------------------------------------------------------------------------------------------- |
| `whitelist`       | Пропускает сообщение, если в тексте или названиях вложений найдена хотя бы одна строка из списка.           |
| `blacklist`       | Блокирует сообщение, если встречена строка из списка.                                                       |
| `allowed_senders` | Разрешает сообщения только от указанных имён, никнеймов или ID.                                             |
| `blocked_senders` | Отбрасывает сообщение, если автор найден в списке.                                                          |
| `allowed_types`   | Ограничивает типы контента (`text`, `attachment`, `image`, `video`, `audio`, `document`, `other`).          |
| `blocked_types`   | Блокирует перечисленные типы контента.                                                                      |

> 🔄 Фильтры можно задавать как глобально, так и в конкретном канале. Локальные настройки **дополняют** глобальные.

### 4. Кастомизация текста (опционально)

```yaml
customization:
  header:
    - "🔥 Важные новости"
    - "#HashTag"
  footer:
    - "— Forward Bot"
    - "Следите за обновлениями"
  replace:
    - find: "LFG"
      replace: "Let's go"
    - find: "OG"
      replace: "Original"
```

- `header` и `footer` принимают строку или список строк. Они объединяются через пустую строку и вставляются до или после текста.
- `replace` можно указать как список блоков `find`/`replace` или как словарь `"что": "на что"`.
- Локальные настройки каналов расширяют глобальные: заголовки и подписи дописываются, а замены перекрываются по ключу.

### 5. Связка каналов Discord с чатами Telegram

```yaml
announcement_channels:
  - discord_channel_id: 123456789012345678
    telegram_chat_id: "-1001234567890"
    filters:
      blacklist:
        - "maintenance"
      allowed_types:
        - text
    customization:
      header: "📢 Анонсы"
      replace:
        TESTNET: "Testnet"

  - discord_channel_id: 223456789012345678  # будет использовать общий telegram_chat_id

pinned_channels:
  - discord_channel_id: 987654321098765432
    telegram_chat_id: "@validators_updates"
    filters:
      whitelist:
        - "guide"
        - "how to"
    customization:
      footer: "📌 Закреплённая информация"
```

- `announcement_channels` — обычные сообщения.
- `pinned_channels` — появление новых закреплённых сообщений.
- Для каждого канала можно задать свои `filters` и `customization`; они дополнят глобальные настройки.

> 📎 **Как получить ID?** В Discord включите «Developer Mode» → ПКМ по каналу → «Copy Channel ID». В Telegram поможет @userinfo_bot или @RawDataBot.

---

<a id="run"></a>
## ▶️ Запуск

```bash
python -m forward_monitor --config config.yml
```

| Параметр            | Описание                                                                                           |
| ------------------- | -------------------------------------------------------------------------------------------------- |
| `--once`            | Делает один проход по всем каналам и завершает работу — удобно для проверки.                       |
| `--log-level DEBUG` | Показывает больше подробностей в логах (по умолчанию используется уровень `INFO`).                 |

Монитор работает бесконечно, делая паузу `poll_interval` секунд между циклами.

---

<a id="reliability"></a>
## 💾 Состояние и надёжность

- В файле `state_file` хранится ID последнего обработанного сообщения или закрепа, что предотвращает повторы.
- Если файл повреждён, создаётся резервная копия `<state_file>.bak`, после чего монитор продолжает работу «с чистого листа».
- Храните файл в доступном месте, если запускаете бота на нескольких серверах или в разных окружениях.

---

<a id="tips"></a>
## 🛠 Полезные советы

- Соблюдайте лимиты Discord: клиент корректно обработает ответ 429 и подождёт, но не запускайте десятки мониторов с одного токена.
- Убедитесь, что у Discord-аккаунта есть доступ к истории сообщений и закрепам всех отслеживаемых каналов.
- В Telegram сообщения пересылаются в порядке: текст → ссылка на оригинал → каждое вложение отдельным сообщением.
- При обновлении конфигурации достаточно перезапустить скрипт — состояние сохранится, повторы не появятся.

---

<a id="questions"></a>
## ❓ Вопросы

Создайте issue в репозитории или свяжитесь с автором, если заметили баг или хотите предложить улучшение. Хороших пересылок! 🚀
